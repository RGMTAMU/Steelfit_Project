<h1>Editing grocerylist</h1>

<%= form_for :grocerylist, url: grocerylist_path(@grocerylist), method: :patch do |f| %>

  <% if @grocerylist.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@grocerylist.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @grocerylist.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </p>
 
  <p>
    <%= f.label :grocerylist, 'Grocery List' %><br>
    <%= f.text_area(:grocerylist, size: "40x10") %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', grocerylists_path %>
